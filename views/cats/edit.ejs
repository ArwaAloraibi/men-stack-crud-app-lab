<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= cat.name %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
</head>
<body>
  <!-- update the header -->
  <h1>Edit <%= cat.name %></h1>
  <form action="/cats/<%=cat._id%>?_method=PUT" method="POST"> 
   
      <label for="name">Type:</label>
      <!-- Use value to pre-fill -->
    <input type="text" name="name" id="name" value="<%= cat.name %>"/>

     <label for="color">color:</label>
     <!-- Use value to pre-fill -->
    <input type="text" name="color" id="color" value="<%= cat.color %>"/>
 
    <div class="checkbox-div">
   <label for="isReadyToAdopt">Ready to adopt?</label>
   <input type="checkbox" name="isReadyToAdopt" id="isReadyToAdopt" <%= cat.isReadyToAdopt ? "checked" : "" %> />
 </div>

    <button type="submit">Update Cat!</button>
  
  </form>

<a class="cats-index-link" href="/cats">
   <div class="index-link-text">Back to Cats</div>
 </a>
 
</body>

</html>